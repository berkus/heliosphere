application: destiny-heliosphere
version: 1
runtime: go
api_version: go1

handlers:
  - url: /static
    static_dir: static

  - url: /favicon\.ico
    static_files: favicon.ico
    upload: favicon\.ico

  - url: /.*
    script: _go_app
    login: required
